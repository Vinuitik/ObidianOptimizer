"""MCP tool implementations, grouped by concern. See mcp_server.py — that
module owns the FastMCP instance and shared DB/vault-path helpers; importing
these submodules registers their @mcp.tool() functions onto it.
"""
