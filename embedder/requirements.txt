# Core serving
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# ONNX inference — GPU-capable; falls back to CPU at runtime if no CUDA present
onnxruntime-gpu>=1.19.0

# Model loading + ONNX export (converts PyTorch → ONNX on first run, then caches)
optimum>=1.21.0
transformers>=4.44.0
torch>=2.3.0          # needed by optimum for the one-time ONNX export; not used at inference time

# Numerics
numpy>=1.26.0

# Tests
pytest>=8.3.0
httpx>=0.27.0         # TestClient transport
pytest-asyncio>=0.23.0
