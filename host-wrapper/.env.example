# OPTIONAL local overrides only — all credentials live in the repo-root .env
# (single source of truth). The wrapper loads ../.env first, then this file
# (this file wins on conflicts). You normally don't need this file at all.

# PORT=5001
# VAULT_HOST_PATH=C:/different/path/than/HOST_VAULT_PATH
# CLI_TIMEOUT_S=180
