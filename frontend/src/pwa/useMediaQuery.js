// Moved to src/utils/useMediaQuery.js (shared by desktop + PWA). Kept here as a
// re-export so existing pwa/ imports keep working.
export { default, MOBILE_QUERY, useIsMobile } from '../utils/useMediaQuery';
